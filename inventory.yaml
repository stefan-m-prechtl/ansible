all:
  hosts:
    #server:
    #  ansible_host: 192.168.178.29

  children:
    test_hosts:
      hosts:
        workstation1:
      vars:
        ansible_user: etienne
        #ansible_become: yes
        #ansible_become_password: '<geheim>'
